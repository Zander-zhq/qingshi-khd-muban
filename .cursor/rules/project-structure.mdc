---
description: 项目结构规范 — 基础框架与产品业务代码的分离规则
globs: 
alwaysApply: true
---

# 项目结构规范

本项目是「青拾客户端模板」，采用基础框架 + 产品业务分离架构。多个产品（视频下载、AI视频、剪辑等）共用此模板，通过 Git upstream 同步基础框架更新。

## 核心规则

### 产品专属代码 — 只在这两个位置修改

1. **`src/app/`** — 产品业务代码（页面、路由）
   - `src/app/pages/` — 业务页面组件
   - `src/app/routes.ts` — 业务路由定义（会被注入到 /main 下作为子路由）

2. **`src-tauri/src/app_config.rs`** — 产品的 APP_ID 和 APP_KEY

### 基础框架代码 — 不要随意修改

以下目录/文件属于基础框架，所有产品共用。修改这些文件会导致 upstream 同步时产生冲突：

- `src/api/` — 通用 API（auth、brand、pay、version）
- `src/composables/` — 通用组合式函数
- `src/components/` — 公共组件（TitleBar 等）
- `src/stores/` — Pinia 状态管理
- `src/utils/` — 工具函数
- `src/templates/` — UI 模板（green/orange/dark）
- `src/dev/` — 开发工具（BrandManager、VersionManager）
- `src/router/index.ts` — 核心路由（导入 app/routes.ts 合并）
- `src/brand.ts` — 品牌配置系统
- `src-tauri/src/lib.rs` — Rust 核心命令

如果需要给基础框架添加新功能，应该在**模板仓库**中修改，然后通过 `git pull upstream master` 同步到各产品。

## 添加新业务页面的正确方式

1. 在 `src/app/pages/` 下创建 Vue 组件
2. 在 `src/app/routes.ts` 中注册路由
3. 路由会自动被合并到 `/main` 下

```typescript
// src/app/routes.ts 示例
const appRoutes: RouteRecordRaw[] = [
  {
    path: 'my-feature',
    name: 'MyFeature',
    component: () => import('./pages/MyFeatureView.vue'),
    meta: { title: `功能名 - ${brand.brand_name}`, requiresAuth: true },
  },
]
```

## 技术约定

- 前端使用 Vue 3 Composition API + `<script setup>` 语法
- 所有 `/client/*` API 请求使用 `src/utils/request.ts` 的 `post()` 函数（自带 HMAC 签名）
- 管理端 API（`/huayun/api/*`）使用 axios 直接调用，token 通过 Body/Form 传递
- APP_ID 和 APP_KEY 在 Rust 端管理（`app_config.rs`），前端通过 `invoke('get_app_credentials')` 获取
- 品牌配置（名称、Logo、模板等）从服务端加密同步，不需要硬编码
- 版本号通过 `VITE_APP_VERSION` 环境变量在构建时注入，不要直接修改 `brand.ts` 中的 VERSION 值
- 使用中文简体进行交流
